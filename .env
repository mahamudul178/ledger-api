# Django settings
DEBUG=True
SECRET_KEY=your-secret-key

# Allowed hosts
ALLOWED_HOSTS=127.0.0.1,localhost

# Database settings
# Use "localhost" for local, "db" for Docker
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=ledger_db
DB_USER=postgres
DB_PASSWORD=postgres
